# TableCharts chart node

Use this when an agent has a table and needs a shareable live chart.

Input: JSON rows, CSV, or a Notion / Google Sheets / Salesforce URL
Output: dashboard_url + embed_iframe

MCP: npx -y @tablecharts/mcp-server
Agent card: https://tablecharts.co/.well-known/agent-card.json
